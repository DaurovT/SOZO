{
  "name": "@sozo/kernel",
  "version": "0.1.0",
  "description": "Ядро монолита (DEV-07): Money (BIGINT тийины), uuid v7, StateMachine — исполнитель графов DEV-10, outbox-интерфейсы. Kernel не знает ни об одном модуле.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sozo/contracts": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
