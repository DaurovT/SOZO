# dispatcher-web — React SPA «Диспетчерская» (PRD-03: 20 экранов D-01…D-20)
Каркас создаётся в M0-E1-S4 (`npm create vite@latest . -- --template react-ts`).
Desktop-first, мин. ширина 1440 px, язык — только русский. Kanban (D-02) строится строго по графам DEV-10; drag между колонками запрещён.
