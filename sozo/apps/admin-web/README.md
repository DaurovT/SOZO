# admin-web — React SPA «Админ + Бухгалтерия» (PRD-04: 38 экранов A-01…A-38)
Каркас создаётся в M0-E1-S4 (`npm create vite@latest . -- --template react-ts`).
Правила: типы API — только из `@sozo/contracts`; токены дизайна — `tokens.css` из DEV-06 (hex в коде один раз); statusColor(status) — единая функция статусных цветов.
